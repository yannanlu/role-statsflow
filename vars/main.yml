---
qbroker_security_plugin: "{{statsflow_security_plugin}}"
qbroker_logdir: "{{statsflow_logdir}}"

influxdb_db_name: "{{statsflow_db_name}}"

nginx_log_locations:
  - "{{statsflow_log_location}}"
nginx_extra_server:
  port: 8082
  server_name: localhost
  docroot: "{{ngnix_default_docroot}}"
